import { StudentUserResponse } from './student-user-response';

describe('StudentUserResponse', () => {
  it('should create an instance', () => {
    expect(new StudentUserResponse()).toBeTruthy();
  });
});
